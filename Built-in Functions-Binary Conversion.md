# Built-in Functions -Binary Conversion Using Built-in Functions in Python

## 🎯 Aim:
To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions.

## 🧠 Algorithm:
1. Assign the value `16` to a variable `a`.
2. Use the built-in `bin()` function to convert the number to binary.
3. Print the result.

## 🧾 Program:
```python
a=16
binary=bin(a)
print("binary of {} is {}".format(a,binary))
```

## Output:
<img width="463" height="167" alt="image" src="https://github.com/user-attachments/assets/01938f61-274d-4f3e-b4e3-ad054cda7221" />


## Result:
Thus, To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions is done successfully.

